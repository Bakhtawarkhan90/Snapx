services:
  database:
    image: mysql:5.7
    container_name: database
    environment:
      MYSQL_ROOT_PASSWORD: kali
      MYSQL_DATABASE: table
    ports:
      - "3306:3306"
    volumes:
      - mysqldata:/var/lib/mysql
    networks:
      - snapx

  flask_app:
    build: .
    container_name: snapx
    image: bakhtawar375/snapx:latest
    environment:
      MYSQL_HOST: database
      MYSQL_USER: root
      MYSQL_PASSWORD: kali
      MYSQL_DATABASE: table
    ports:
      - "5000:5000"
    networks:
      - snapx
    depends_on:
      - database

volumes:
  mysqldata:

networks:
  snapx:
